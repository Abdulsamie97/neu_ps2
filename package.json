{
  "name": "pseudo2-workspace",
  "description": "Base workspace package",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "npm run clean --workspaces",
    "compile": "tsc -b tsconfig.build.json",
    "watch": "tsc -b tsconfig.build.json --watch",
    "build": "npm run build --workspace packages/extension && npm run compile && npm run build --workspace pseudo2-standalone-web && npm run build --workspace pseudo2-vpl",
    "build:web": "npm run build && vite build",
    "build:standalone": "npm run build --workspace pseudo2-language && npm run build --workspace pseudo2-standalone-web",
    "build:vpl": "npm run build --workspace pseudo2-language && npm run build --workspace pseudo2-vpl",
    "build:clean": "npm run clean && npm run build",
    "dev": "vite",
    "preview:web": "vite preview --host 127.0.0.1 --port 4173",
    "langium:generate": "npm run --workspace packages/language langium:generate",
    "langium:watch": "npm run --workspace packages/language langium:watch",
    "test": "npm run --workspace packages/language test && npm run --workspace packages/cli test && npm run test --workspace pseudo2-standalone-web && npm run test --workspace pseudo2-vpl",
    "test:direct-c-corpus": "npm run build --workspace pseudo2-language && vitest run packages/language/test/integration/DirectCXtextExamples.test.ts packages/cli/test/generator/DirectCXtextCorpus.test.ts"
  },
  "devDependencies": {
    "@types/node": "~20.17.48",
    "shx": "~0.4.0",
    "typescript": "~5.8.3",
    "@codingame/esbuild-import-meta-url-plugin": "~1.0.3",
    "@codingame/monaco-vscode-rollup-vsix-plugin": "^23.0.0"
  },
  "volta": {
    "node": "20.19.2",
    "npm": "10.8.2"
  },
  "workspaces": [
    "packages/extension",
    "packages/language",
    "packages/cli",
    "packages/web",
    "packages/standalone-web",
    "packages/vpl"
  ]
}
